# Categorizador de sentiment de comentarios usando ML
En este código se utiliza el modelo robetuito para clasificar por sentiment comentarios de redes sociales (FB, IG, X, etc...)

## Instalar dependencias
```python
pip install robertuito
```
